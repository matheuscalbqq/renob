name: CI/CD

# Dispara em push e PR na main e permite execução manual
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-test:
    name: 📦 Build & Test
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout do repositório (precisa do histórico completo para o deploy depois)
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Instala e cacheia o Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      # 3. Instala dependências
      - name: Install dependencies
        run: npm ci

      # 4. Executa o build
      - name: Build
        run: npm run build

      # 5. Deploy para GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          # para o primeiro deploy, permite criar um commit vazio
          allow_empty_commit: true
          # configurações de usuário para o commit
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'